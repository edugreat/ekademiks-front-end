import { OptionSortPipe } from './option-sort.pipe';

describe('OptionSortPipe', () => {
  it('create an instance', () => {
    const pipe = new OptionSortPipe();
    expect(pipe).toBeTruthy();
  });
});
